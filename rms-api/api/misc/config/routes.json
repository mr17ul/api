{
    "routes": [
        {
            "method": "POST",
            "path": "/recaptcha",
            "handler": "misc.validateRecaptcha",
            "config": {
                "policies": []
            }
        }
    ]
}